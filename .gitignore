# Build output
target/

# IDE local files (keep the shared project files under .idea/)
.idea/vcs.xml
.idea/claudeCodeTabState.xml

# PDF extraction artifacts (local reference only)
pdf_extract/

# Local project copy used for comparison (do not track)
reggie_take_out对比/

# Local audit reports / smoke test scripts (do not track)
audit/
